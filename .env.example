# ================================================================
# DDQ Agent - Environment Configuration
# ================================================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Generate FERNET_KEY: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# 3. Replace the values below with your own
# 4. Save the file
# 5. Run: docker-compose up --build
#
# ================================================================

# Database Password
# Use a strong password (16+ characters, mix of letters, numbers, symbols)
DB_PASSWORD=your_secure_password_here

# Encryption Key for User API Keys
# Generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# IMPORTANT: Keep this secret! It's used to encrypt/decrypt OpenAI API keys
FERNET_KEY=your_fernet_key_here

# Example Fernet key (DO NOT use in production):
# FERNET_KEY=a0H07YOiGE6x2hC4sBYOlsG6BosGxDDMhmXMBegXp_g=

# ================================================================
# NOTES:
# ================================================================
# - DB_PASSWORD: Used for PostgreSQL database authentication
# - FERNET_KEY: Used to encrypt user OpenAI API keys in database
# - OpenAI API keys are entered by users through the UI (not here)
# - All other configuration is handled by docker-compose.yml
# ================================================================
